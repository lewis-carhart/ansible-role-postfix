# Track client email sends
/^Feedback-ID:/ "{{ item.key.feedback_id }}"

# Send to a different SMTP server if X-Maru-Priority: true
/^X-Maru-Priority:\s*true/ FILTER smtp:{{item.key.priority_filter}}
