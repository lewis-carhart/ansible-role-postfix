---
  postfix:
    main:
      soft_bounce: "no"
      queue_directory: "/var/spool/postfix"
      command_directory: "/usr/sbin"
    master:
      test1: 1001
    header_checks:
      feedback_id: WARN
      priority_server: 10.0.1.16
